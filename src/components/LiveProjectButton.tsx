import React from 'react';

export const LiveProjectButton = () => {
  return (
    <button className="rounded-full border-2 border-text-primary text-text-primary font-medium uppercase tracking-widest px-8 py-3 sm:px-10 sm:py-3.5 text-sm sm:text-base hover:bg-[var(--text-primary)]/10 transition-colors whitespace-nowrap">
      Live Project
    </button>
  );
};
